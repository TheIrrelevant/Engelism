import type { WizardState } from '../types/wizard'
import type { Library } from '../types/library'

/**
 * Build the prompt for AI vision analysis.
 * Full implementation in Step 07.
 */
export function buildPrompt(_state: WizardState, _lib: Library): string {
  return 'TODO: implement in Step 07'
}
