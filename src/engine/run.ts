/**
 * Multi-provider AI engine entry point.
 * Full implementation in Step 07.
 */
export async function runEngelEngine(_prompt: string, _image: File): Promise<string> {
  throw new Error('Engine not yet implemented — see Step 07')
}
